MuhStatsConfig = {
    -- Host to connect to
    Host = "http://localhost:3306",

    -- Should the http client verify the tls connection
    VerifyCertificate = false,

    -- Http request/response timeout in seconds
    Timeout = 5,
}