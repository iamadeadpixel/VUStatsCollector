-- Here you can turn on, or off modules.
Config = {
	humansonly = true, -- true = Only humans are logged / false = includes bots in logging.
	updatecheck = false -- if true, it checks for updates (disabled due ises)
}

return Config
